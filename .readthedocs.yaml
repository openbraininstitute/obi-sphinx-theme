# Read the Docs configuration file for obi-sphinx-theme
version: 2

sphinx:
  configuration: doc/source/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

python:
  install:
    - requirements: doc/requirements.txt
    - method: pip
      path: .
